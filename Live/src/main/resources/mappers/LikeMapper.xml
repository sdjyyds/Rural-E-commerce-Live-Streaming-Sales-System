<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sdjyyds.live.mapper.LikeMapper">
    <insert id="insert">
        INSERT INTO likes (id, user_id, stream_id, created_at)
        VALUES (#{id}, #{userId}, #{streamId}, NOW())
    </insert>
</mapper>