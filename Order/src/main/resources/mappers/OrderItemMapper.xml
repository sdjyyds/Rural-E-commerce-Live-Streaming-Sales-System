<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sdjyyds.order.mapper.OrderItemMapper">
    <insert id="insert" >
        INSERT INTO order_items (id,order_id, product_id,product_name,product_image,quantity, unit_price,actual_price, created_at, updated_at)
        VALUES (#{id},#{orderId}, #{productId},#{productName},#{productImage}, #{quantity}, #{unitPrice},#{actualPrice}, #{createdAt}, #{updatedAt})
    </insert>
</mapper>